backend = "glx";
vsync = true;
blur:
{
    method = "gaussian";
    size = 10;
    deviation = 5.0;
};

glx-use-copysubbuffer-mesa = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;
